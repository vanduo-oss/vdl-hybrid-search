# Changelog

## 0.1.1

- Add optional `onnxWasmPaths` for CSP hosts (same-origin ORT WASM).

## 0.1.0

- Initial promotion from vanduo-oss/labs (`neptune-search.js` → `HybridSearch`) as a standalone TypeScript package.
